
Проект 35. 

Создать базовый и производный класс полиморфной иерархии именем своей фамилии и имени, распределив их по своим модулям h/cpp.
Создать в базовом классе член-данных  m_txt  правильного  типа  и инициализировать его прочитанными данными из подготовленного заранее файла с текстом (UTF-8) басни И.А.Крылова «Стрекоза и муравей» (прочесть первую половину басни). В производном классе член-данных с подобным же именем: m_txt. Его заполнить данными из второй половины того же файла.
Реализовать отдельный от иерархии шаблонный класс базы данных DB<T>, который внутри содержит vector <T>. 
В функции main через random создать 6-8 объектов базового и производного типов и положить их в объект типа DB, а затем скопировать в другой объект DB.
Далее последовательно удалить через erase объекты из второй БД с условием, чтобы остались внутри нее только один объект базового класса и один - производного. К  объекту производного  типа применить, выводя m_txt :
 wcout<< db2;
Должна быть распечатана, таким образом, вся басня 1 раз.	